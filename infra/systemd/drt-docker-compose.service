[Unit]
Description=DRT production stack (Docker Compose)
Requires=docker.service
After=docker.service network-online.target
Wants=network-online.target

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/azureuser/DRT_Design_Document/infra
# Run as a user in the docker group (not root); change User= to your deploy user.
User=azureuser
Group=azureuser
# Use up -d only on boot; run --build manually when deploying code changes.
# drt-test: add --profile testing to ExecStart so Mailpit starts.
# Omit that profile on a production host or Mailpit will catch real mail.
ExecStart=/usr/bin/docker-compose -f docker-compose.prod.yml --env-file ../.env.production up -d
# Use `stop` (not `down`) so volumes/networks are not removed on service stop.
ExecStop=/usr/bin/docker-compose -f docker-compose.prod.yml --env-file ../.env.production stop -t 60
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
